## 1. SKILL.md

---
name: obsidian-fire-rename
description: 根据 Obsidian 知识库文件内容自动规范化文件名称。当用户要求"重命名文件"、"规范化文件名"、"文件命名检查"、"fire rename"、"批量重命名"时使用。读取文件内容 → 按命名规则生成新文件名 → 重命名 → 记录日志。适用于 Obsidian vault 中的 .md 文件。
---

# Obsidian Fire Rename

根据文件内容自动规范化 Obsidian 知识库文件名称。

**旁路工具**：不在 Pipeline 主流程中，仅在批量重命名已有文件时使用。

## 双层架构

| 层级 | 文件 | 用途 | 写入者 |
|------|------|------|--------|
| 人类可读层 | `📖目录 索引.md` | 浏览导航 | Mulu-fenlei + Organizer |
| 机器可读层 | `logs/rename/rename-state.json` | 记录原文件名、新文件名、处理状态 | Fire-rename |

**读取优先级**：
1. 先读状态文件（快速跳过已处理文件）
2. 再扫描未处理文件

## 工作流程

```
遍历目录文件
    ↓
检查 rename-state.json（跳过已处理文件）
    ↓
判断是否为系统文件（跳过）
    ↓
读取 frontmatter + 前30行内容
    ↓
按命名规则生成新文件名
    ↓
执行重命名
    ↓
双链维护（扫描其他文件中的 [[旧文件名]] → 更新为 [[新文件名]]）
    ↓
更新状态文件
```

## 第一步：检查状态文件，过滤已处理文件

读取 `logs/rename/rename-state.json`，提取所有已记录的原文件名和新文件名。
只处理没有记录的文件。跳过已记录的文件。

## 第二步：判断系统文件（跳过不改）

以下文件保持原名，不进入命名流程：

- `AGENTS.md`、`CLAUDE.md`、`README.md`、`AI_INDEX.md`
- `🏠 home-*.md`、`🤖 AI指令.md`、`🧩 目录-*.md`
- `.gitignore`、`.env`、`package.json`、`tsconfig.json`
- `🍕 作业区.md`、`opencode.jsonc`
- `templates/`、`聊天记录/` 目录下的文件

## 第三步：读取文件内容

对每个非系统文件，读取 frontmatter + 前30行，提取：
1. **标题**（第一个 `#` 开头的行）
2. **核心对象**（文件主要讲谁/什么）
3. **文件类型**（知识/项目/工具）
4. **保存价值**（这个文件有什么价值？）

## 第四步：按命名规则生成新文件名

详细规则见 [references/naming-rules.md](references/naming-rules.md)。

### 核心公式

| 文件类型 | 格式 | 示例 |
|---------|------|------|
| 普通文件 | `主题 - 类型 @ 说明` | `Docker - 工具 @ 环境部署方法` |
| 高级文件 | `领域 - 主题 - 类型 @ 说明` | `AI - Agent - 知识 @ 智能体运行机制` |
| 项目文件 | `项目 - 文档类型 - 状态 @ 说明` | `LEO OS - 项目文档 - 定稿 @ AI认知系统设计` |

### 主题规则
- 使用明确实体名称（ChatGPT、Python、Docker）
- 不加情绪词（超级、最强、重要）
- 保留行业通用关键词

### 类型词库（固定，不要创造）

**知识页面类**：知识、概念、原理、笔记、总结、对比、案例

**工具配置类**：工具、教程、指南、手册、清单、安装、配置

**项目文档类**：项目文档、需求文档、设计方案、技术方案、会议记录、分析报告

### @说明规则
- 不重复标题，回答"这个文件有什么价值"
- 用途说明：`@ 用于xxx`
- 内容说明：`@ 包含xxx`
- 场景说明：`@ xxx场景使用`
- 关联说明：`@ 关联xxx`

### 状态字段（仅项目文件使用）
草稿、初稿、V1、V2、修订版、测试版、定稿、正式版、归档

## 第五步：执行重命名

使用 PowerShell `Rename-Item` 命令重命名文件。注意 UTF-8 编码。

## 第六步：双链维护（强制，避免双链失效）

**重命名后必须扫描整个 vault 中其他文件的双链引用，把 `[[旧文件名]]` 更新为 `[[新文件名]]`，否则原链接会变成"未找到引用"的红色断链。**

### 扫描范围

整个 `D:\Obsidian\LeoDiary\` 下的所有 `.md` 文件，排除：
- `.trash/`、`node_modules/`、`.git/`、`logs/`
- 已处理的源文件自身

### 扫描命令

```powershell
# 提取旧文件名（不含扩展名）和新文件名（不含扩展名）
$旧名 = [System.IO.Path]::GetFileNameWithoutExtension("原文件名.md")
$新名 = [System.IO.Path]::GetFileNameWithoutExtension("新文件名.md")

# 在所有 md 文件中搜索 [[旧名]] 引用
Select-String -Path "D:\Obsidian\LeoDiary\*.md" -Pattern "\[\[$旧名\]\]" -Recurse
```

### 更新规则

| 情况 | 处理 |
|------|------|
| 找到 `[[旧文件名]]` | 替换为 `[[新文件名]]`（保留前后别名，如 `[[旧文件名\|别名]]` → `[[新文件名\|别名]]`） |
| 找到 `[[旧文件名|别名]]` | 仅替换文件名部分，保留别名 |
| 找到 `![[旧文件名]]`（图片嵌入） | 也替换为新文件名（防止资源嵌入断裂） |
| 未找到引用 | 跳过（无需更新） |

### 批量更新效率

为避免对每个重命名文件都全量扫描，**批量重命名完成后一次性扫描所有更名**：

```
遍历所有重命名记录（旧名→新名）
    ↓
对 vault 中每个 .md 文件，按所有 (旧名, 新名) 对做替换
    ↓
记录每文件被更新的链接数
```

### 双链更新记录

更新的双链必须记录到状态文件（见下方"双链更新"字段）和运行日志：

```markdown
### YYYY年M月D日
- 【双链更新】旧文件名 → 新文件名（影响 X 个文件，更新 X 处引用）
  - 受影响文件：[[文件1]]、[[文件2]]、[[文件3]]
```

## 第 6.5 步：✍️ 摘要同步维护（目录文件 + 索引）

**重命名后，目录文件和索引中的 `[[文件名]] ✍️ 摘要` 条目需要同步处理。**

| 场景 | 处理方式 |
|------|---------|
| 双链已替换，✍️ 摘要仍匹配 | 保留不动（默认情况，文件改名但核心内容没变） |
| 双链已替换，✍️ 摘要不再匹配 | 更新 ✍️ 摘要内容（从源文件 `>✍️` 行读取） |
| 目录文件中找不到该条目 | 跳过（该目录未收录此文件） |

### 检查优先级

1. **先读源文件的 `>✍️` 行**：如果源文件 H1 下方已有 `>✍️ 一句话总结`，直接复用
2. **再检查目录文件条目**：`[[新文件名]] ✍️` 后面的内容与源文件 `>✍️` 是否一致
3. **不一致才更新**：一致则跳过，避免无意义修改

### 同步范围

| 文件 | 更新内容 |
|------|---------|
| `🧩 目录-xxx.md` | `[[旧名]] ✍️ xxx` → `[[新名]] ✍️ xxx`（双链已替换，✍️ 按需更新） |
| `📖目录 索引.md` | 同上 |

## 第七步：更新状态文件

更新 `logs/rename/rename-state.json`：

```json
{
  "原文件路径": {
    "原文件名": "原文件名.md",
    "新文件名": "新文件名.md",
    "目录": "目标目录",
    "处理状态": "已重命名",
    "处理时间": "ISO 8601 时间戳",
    "双链更新": {
      "受影响文件数": 0,
      "更新链接数": 0,
      "受影响文件": ["文件路径列表"]
    }
  }
}
```

## 常见问题处理

| 问题 | 处理方式 |
|------|---------|
| 文件名已有 `主题 - 类型 @ 说明` 格式 | 检查 @说明 是否准确，不准确则调整 |
| 文件名含情绪词/口语词 | 去除，保留核心主题 |
| 标题过长含冗余信息 | 精简，保留核心 |
| 缺少 @说明 字段 | 根据内容补充 |
| 缺少类型字段 | 根据内容判断类型并补充 |
| 重命名后双链失效（红色断链） | 第六步双链维护未执行 → 重新扫描全 vault 并更新 `[[旧文件名]]` → `[[新文件名]]` |
| 双链带别名 `[[旧名\|别名]]` | 仅替换文件名部分，保留别名分隔符 `\|` 后内容 |
| 图片嵌入 `![[旧名]]` 也需更新 | 一并替换为新名，避免资源嵌入断裂 |

## 操作记录

所有重命名操作必须记录到 `⚓新增文件记录.md`，格式如下：

```markdown
### YYYY年M月D日
- 【优化】原文件名 → 新文件名 D:\Obsidian\LeoDiary\路径\
```

追加写入，不覆盖已有记录。

---

## 状态追踪机制

### 追踪文件位置

```
logs/rename/rename-state.json
```

### 追踪数据结构

```json
{
  "版本": "1.0",
  "上次运行": "ISO 8601 时间戳",
  "文件状态": {
    "原文件路径": {
      "原文件名": "原文件名.md",
      "新文件名": "新文件名.md",
      "目录": "目标目录",
      "处理状态": "已重命名 | 已跳过",
      "处理时间": "最后处理时间",
      "双链更新": {
        "受影响文件数": 0,
        "更新链接数": 0,
        "受影响文件": ["文件路径列表"]
      }
    }
  }
}
```

### 变化检测规则

| 条件 | 判断 | 动作 |
|------|------|------|
| 文件名已符合 `主题 - 类型 @ 说明` 格式 | 已规范 | 跳过 |
| 文件名不符合格式 | 需规范 | 重命名 |
| 文件不在追踪记录中 | 新增 | 处理 |
| 文件在追踪中且名称未变 | 未变化 | 跳过 |

### 执行后必须

1. 更新 `logs/rename/rename-state.json`
2. 写入运行日志 `logs/rename/rename-YYYY-MM-DD.md`
3. 生成可执行的回滚脚本（见下方）

## 回滚机制（强制）

**每次重命名必须生成可执行的 PowerShell 回滚脚本，否则视为不完整执行。**

### 回滚脚本路径

```
logs/rename/rename-rollback-YYYY-MM-DD-HHMM.ps1
```

### 回滚脚本内容

```powershell
# Fire-rename 回滚脚本
# 生成时间: YYYY-MM-DD HH:MM
# 用法: powershell -ExecutionPolicy Bypass -File rename-rollback-YYYY-MM-DD-HHMM.ps1

$ErrorActionPreference = 'Stop'

# 1. 文件名回滚（旧名 → 新名 的逆操作）
$renameMap = @(
    @{ 新名 = "新文件名1.md"; 旧名 = "旧文件名1.md"; 目录 = "目录路径1" }
    @{ 新名 = "新文件名2.md"; 旧名 = "旧文件名2.md"; 目录 = "目录路径2" }
)

foreach ($r in $renameMap) {
    $newPath = Join-Path $r.目录 $r.新名
    $oldPath = Join-Path $r.目录 $r.旧名
    if (Test-Path $newPath) {
        Move-Item -LiteralPath $newPath -Destination $oldPath -Force
        Write-Host "[回滚] $($r.新名) → $($r.旧名)"
    } else {
        Write-Host "[跳过] $($r.新名) 不存在"
    }
}

# 2. 双链回滚（新名 → 旧名 的逆操作）
# 使用 Get-ChildItem + Select-String + (Get-Content | ForEach-Object { $_ -replace '\[\[新名', '[[旧名' } | Set-Content) 批量回滚
$doubleLinkMap = @(
    @{ 新名 = "新文件名1"; 旧名 = "旧文件名1" }
)

$vaultRoot = "D:\Obsidian\LeoDiary"
$excludeDirs = @("_trash", "logs", ".git", "node_modules")
$mdFiles = Get-ChildItem -Path $vaultRoot -Filter "*.md" -Recurse -File | Where-Object {
    $excludeDirs -notcontains $_.Directory.Name
}

foreach ($m in $doubleLinkMap) {
    foreach ($f in $mdFiles) {
        $content = Get-Content -LiteralPath $f.FullName -Raw
        if ($content -match "\[\[$($m.新名)") {
            $newContent = $content -replace "\[\[$($m.新名)", "[[$($m.旧名)"
            Set-Content -LiteralPath $f.FullName -Value $newContent -NoNewline
            Write-Host "[双链回滚] $($f.Name) 中的 [[$($m.新名) → [[$($m.旧名)"
        }
    }
}

Write-Host "==== 回滚完成 ===="
```

### 回滚触发方式

| 触发方式 | 说明 |
|---------|------|
| 用户说"回滚 rename" / "撤销重命名" | 找到最新的 rollback 脚本并执行 |
| 用户指定具体脚本 | `回滚 rename-rollback-2026-07-25-1930.ps1` |
| 自动触发 | merge 完整性验证失败时，自动回滚最近一次 rename |

### 回滚保护

- 回滚脚本必须先备份当前状态到 `logs/rename/backups/before-rollback-YYYY-MM-DD-HHMM.json`
- 回滚前提示用户确认（除非用户明确说"强制回滚"）
- 回滚完成后，更新 state.json 中相关文件的"处理状态"为"已回滚"


---

## 2. SKILL.md

---
name: obsidian-knowledge-compiler
description: 将原始AI输出内容编译为标准化Obsidian知识资产；兼具Markdown结构生成能力+文档格式校验能力，统一知识库排版规范。触发词：内容优化、优化内容、整理内容、内容整理、格式化、编译内容、知识编译。YAML字段统一使用中文，frontmatter与正文之间空一行。
---

# Obsidian Knowledge Compiler

内容编译引擎：接收原始内容 → 类型判断 → 结构搭建 → 格式校验 → 输出标准文档。

**直接用正确文件名创建文件，输出到 `B🧹 整理（Organize）/`。**

上游：`obsidian-knowledge-planner`（接收 YAML contract）
下游：`obsidian-knowledge-organizer`（归属移动）

## 双层架构

| 层级 | 文件 | 用途 | 写入者 |
|------|------|------|--------|
| 人类可读层 | `📖目录 索引.md` | 快速判断"哪些整理了/没整理" | Organizer + Mulu-fenlei |
| 机器可读层 | `logs/compiler/compiler-state.json` | 记录编译结果、动作、目标文件 | Compiler |

**读取优先级**：
1. 先读 YAML（快速获取 Planner 规划结果）
2. 再读状态文件（精确检测文件变化）

## 执行工具

- Read、Write、Edit、Delete、Glob、Grep、Bash

## 输入

### 来源 1：Pipeline 传递（标准流程）

接收 Pipeline 传递的 YAML Contract + 扫描结果。

### 来源 2：单独调用

用户直接说"优化这个文件"、"编译这个文件"。

### 来源 3：Pipeline 标记"内容补充模式"

Pipeline 检测到空壳文件（只有标题，正文很少），要求 Compiler 基于标题搜索相关信息补充内容。

## 工作流程

```
读取 YAML Contract（或直接接收原始文件） → 判断动作类型（create/merge/link） → 内容补充检测 → 编译/合并/补充 → 输出到 B🧹 整理（Organize）/ → 记录
```

## 动作判断

**根据 Planner YAML 中的动作类型决定处理方式：**

| 动作 | 处理方式 | 输出 |
|------|----------|------|
| `create` | 正常编译流程 | B🧹 整理（Organize）/ + state.json 更新 |
| `merge` | 合并到已有文件 | 直接写回目标文件 + state.json 更新 |
| `link` | 仅建立双链，不创建文件 | state.json 更新 |

**单独调用时**，根据文件特征自动判断动作类型：

| 文件特征 | 推测动作 |
|----------|---------|
| 新内容，与已有文件相似度高 | merge |
| 新内容，无相似文件 | create |
| 关联性强但内容独立 | create（附 link 建议） |

## 内容补充模式（内容补充 + merge 到已有文件）

**当 Planner 标记 `模式: 内容补充` 时，说明源文件是空壳文件（只有标题，正文很少），Compiler 需要处理：**

### 两种处理路径

| 情况 | 动作 | 说明 |
|------|------|------|
| **空壳文件有明确标题，且已有对应页面** | merge 到已有文件 | 在已有文件补充内容或新增章节 |
| **空壳文件有明确标题，无对应页面** | create（内容补充） | 搜索补充完整内容后创建新文件 |

#### 判断条件

Planner 传入：
- `动作: create` 或 `动作: merge`
- `模式: 内容补充`
- `源文件`: 空壳文件（< 10 字节或正文极少）

#### 处理流程

```
解析标题提取关键词 → 检测是否有已有页面 → 有则 merge 到已有文件 / 无则搜索补充 → 输出
```

#### merge 到已有文件（空壳补充）

当已有对应页面时，直接将空壳文件的主题作为新章节补充到已有文件中：

| 情况 | 处理 |
|------|------|
| 文件名描述的是已有文件的**补充信息**（如风控问题） | 在已有文件新增对应章节 |
| 文件名描述的是已有文件未包含的**新主题** | 追加到文件末尾 |
| 文件名与已有文件主题重复 | 无需补充，仅记录来源 |

**示例**：`OmniRoute 不太行 太容易触发风控了.md` → 在 `OmniRoute - 工具` 新增「风控问题」章节

#### 搜索补充模式（create + 内容补充）

当无已有页面时，基于标题搜索补充完整内容：

#### 搜索策略

| 标题类型 | 搜索关键词 | 内容结构 |
|----------|-----------|----------|
| 工具/软件 | "软件名 功能 下载 教程" | 介绍 + 核心功能 + 下载安装 + 使用教程 + 常见问题 |
| 概念/知识点 | "概念名 定义 原理 应用" | 定义 + 原理 + 应用场景 + 实践方法 |
| 人物/项目 | "名称 介绍 背景 成果" | 背景介绍 + 核心内容 + 影响意义 |

#### 内容要求

1. **不少于 800 字**的结构化内容
2. 必须包含：简介/定义、核心功能/要点、使用方法/实践、相关资源
3. 标注信息来源（搜索结果参考）
4. 按照对应模板输出（工具类用工具模板、知识类用知识模板）
5. frontmatter 中的 `来源` 字段注明"内容补充（基于标题搜索）"

#### 什么情况不补充（退回 discard）

- 标题完全无法理解（如"未命名"、随机字符）
- 搜索不到任何有效信息
- 内容属于个人化/无法公开查询的（如"我的会议记录"）

退回时输出：`动作: discard` + `原因: 无法通过搜索补充有效内容`

## 合并模式

**当动作为 `merge` 时，执行以下流程：**

### 合并流程

```
备份 → 对比 → 合并 → 写回 → 验证
```

#### 1. 备份

合并前必须备份目标文件到 `logs/compiler/backups/`。

#### 2. 对比

读取目标文件现有内容，与新内容对比：

| 情况 | 处理 |
|------|------|
| 新内容覆盖旧内容的子集 | 跳过（已有完整内容） |
| 新内容包含旧内容没有的信息 | 合并 |
| 新内容与旧内容完全不同 | 追加为独立章节 |

#### 3. 合并

| 合并策略 | 说明 |
|---------|------|
| 追加 | 内容不重叠，追加到文件末尾 |
| 补充 | 内容重叠，补充到已有章节 |
| 重建 | 旧内容太旧/太乱，整体替换 |

#### 4. 写回

写回文件到目标路径。

#### 5. 验证

合并完成后必须验证：

- 目标文件大小 > 100 字节（防止假合并）
- 目标文件第一行是 `# 标题`
- 原始内容未被删除

### 合并规则

| 维度 | 要求 |
|------|------|
| 内容重叠度 > 70% | 跳过（已有完整内容） |
| 内容重叠度 30-70% | 补充（仅添加新内容） |
| 内容重叠度 < 30% | 追加（另起章节） |

### 合并安全规则

| 规则 | 说明 |
|------|------|
| 禁止删除已有内容 | 只添加，不删除 |
| 禁止覆盖图片引用 | 保留原有图片链接 |
| 禁止修改 frontmatter | 只合并正文 |
| 合并后文件大小 | ≤ 500 行（超过则输出警告） |

## 跳过判断

以下文件直接跳过：

| 条件 | 处理 |
|------|------|
| 文件名以 `🧩 目录-` 开头 | 跳过 |
| 文件名以 `🏠 home-` 开头 | 跳过 |
| 文件名为 `README.md` / `CLAUDE.md` / `AGENTS.md` | 跳过 |

## 内容编译规则

### 图片处理

**图片 100% 保留，保持原位。** 禁止移动、删除、替换图片链接。

### 分段处理

连续无分段 > 4 行 → 拆小标题 + 列表。

### 内容扩充

内容过少（< 3 行）→ 搜索补充。

### 内容安全

**禁止**：
- 删减有效信息
- 篡改事实
- 编造内容
- 恶意修改

### 内容补充（非空壳）

非空壳但内容过少时，基于正文关键词搜索补充。

## 文档模板

### 知识模板

```markdown
---
type: knowledge
tags: [关键词, 知识]
created: YYYY-MM-DD
modified: YYYY-MM-DD
---

# 标题 - 知识 @ 说明

>✍️ 一句话总结

## 定义

核心概念定义

## 原理

工作原理

## 应用场景

实践应用

## 相关内容

- 链接
```

### 项目模板

```markdown
---
type: project
status: 进行中
tags: [项目, 关键词]
created: YYYY-MM-DD
modified: YYYY-MM-DD
---

# 标题 - 项目文档 @ 说明

>✍️ 一句话总结

## 背景

## 目标

## 方案

## 进度

## 相关问题
```

### 工具模板

```markdown
---
type: tool
tags: [工具, 关键词]
created: YYYY-MM-DD
modified: YYYY-MM-DD
---

# 标题 - 工具 @ 说明

>✍️ 一句话总结

## 简介

## 核心功能

## 安装/配置

## 使用方法

## 常见问题

## 相关资源
```

**merge 模式不使用模板**，基于已有文件结构智能合并。

## ✍️ 摘要

**Compiler 生成文件时即写好 `>✍️ 详细版摘要`（50-100字），放在 H1 下方。**

Mulu-fenlei-summary 仅补漏，不重新生成已有 ✍️ 的文件。

## 格式校验

输出前检查：

| 检查项 | 要求 |
|--------|------|
| frontmatter | 完整，type/tags/created/modified |
| H1 标题 | 与文件名一致 |
| ✍️ 摘要 | 存在，50-100字 |
| 标题层级 | 不跳级（## → #### 跳过 ###） |
| 图片 | 未移动，保留原链接 |
| 内容 | 完整，无截断 |

## 命名规则

**直接用正确文件名创建文件，输出到 `B🧹 整理（Organize）/`。**

| 类型 | 格式 | 示例 |
|------|------|------|
| 知识 | `主题 - 知识 @ 说明` | `Claude - 知识 @ API配置方法` |
| 项目 | `项目 - 项目文档 @ 说明` | `业委会 - 项目文档 @ 成立方案` |
| 工具 | `主题 - 工具 @ 说明` | `Docker - 工具 @ 容器部署方法` |

## 编码规则

UTF-8 无 BOM。

- **禁止加 BOM**
- Python 用 `utf-8-sig` 读取（兼容 BOM）
- 写文件时用 `utf-8`（不加 BOM）

## 状态追踪

### 追踪文件位置

```
logs/compiler/compiler-state.json
```

### 追踪数据结构

```json
{
  "版本": "1.0",
  "上次运行": "ISO 8601 时间戳",
  "文件状态": {
    "源文件路径": {
      "源文件": "源文件名.md",
      "目标文件": "目标文件名.md",
      "目标路径": "B🧹 整理（Organize）/目标文件名.md",
      "处理状态": "已编译 | 已合并 | 已跳过 | 处理失败",
      "处理时间": "ISO 8601 时间戳",
      "动作": "create | merge | link",
      "内容长度": "字节数",
      "章节数": "编译后的章节数量"
    }
  }
}
```

### 变化检测规则

| 条件 | 判断 | 动作 |
|------|------|------|
| 文件不在追踪记录中 | 新增 | 处理 |
| 文件在追踪中且内容未变 | 未变化 | 跳过 |
| 文件在追踪中但内容已变 | 修改 | 重新编译 |

### 执行后必须

1. 更新 `logs/compiler/compiler-state.json`
2. 写入运行日志 `logs/compiler/compiler-YYYY-MM-DD.md`


---

