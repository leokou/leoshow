## 1. SKILL.md

---
name: obsidian-knowledge-organizer
description: 组织 Obsidian 知识页面：归属目录判断、文件移动、双向链接、索引维护、目录分类更新。触发词：organize knowledge, 整理知识, 移动文件, 建立双链, 更新目录索引, 知识归属, 整理 xxx 目录（如"整理 AI 目录"）。注意："分类目录 xxx"或"文件摘要"触发 Mulu-fenlei-summary，不触发本 Skill。
---

# Obsidian Knowledge Organizer

组织执行引擎：接收 Planner 规划结果 → 归属目录 → 建立双链 → 维护索引 → 更新目录 → 记录日志。

上游：`obsidian-knowledge-planner` → `obsidian-knowledge-compiler` → 本 skill
下游：无（最终输出）

**合并 Mulu-fenlei 职责，移动文件后直接更新目录，不触发 Mulu-fenlei。**

## 与相关 Skill 的职责边界

| 场景 | 用哪个 | 为什么 |
|------|--------|--------|
| 移动文件 + 建双链 + 更新目录 | **Organizer（本 Skill）** | 知识归位整理，涉及文件物理移动 |
| 目录文件分类 + ✍️ 摘要补漏 | **Mulu-fenlei-summary** | 纯目录整理，不移动文件 |
| 批量重命名 + 双链维护 | **Fire-rename** | 改名不改内容 |
| 旧版 Mulu-fenlei | ❌ 已废弃 | 用上面的替代 |

**简单判断**：文件要动位置 → Organizer；目录要整理/摘要要补 → Mulu-fenlei-summary；名字要改 → Fire-rename。

## 双层架构

| 层级 | 文件 | 用途 | 写入者 |
|------|------|------|--------|
| 人类可读层 | `📖目录 索引.md` + `🧩 目录-xxx.md` | 浏览导航、快速判断处理状态 | Organizer（移动后直接更新） |
| 机器可读层 | `logs/organizer/organizer-state.json` | 记录原路径、目标路径、风险等级、回滚脚本 | Organizer |

**读取优先级**：
1. 先读索引（快速跳过已处理文件）
2. 再读状态文件（精确检测路径变化）

## 执行工具

- Read、Write、Edit、Delete、Glob、Grep、Bash

移动文件使用 Bash：`mv "old_path" "new_path"` 或 Read+Write+Delete 组合。

## 核心原则

1. **先理解结构，再决定归属**：读取 📖目录 索引.md → 定位领域 → 缩小范围 → 精准匹配
2. **先轻匹配，再深分析**：标题 → 路径 → Frontmatter → 标签 → 正文（逐级深入）
3. **强关联进入知识体系**：关联强 → 归档正式目录
4. **无法归属进入整理区**：无法明确归属 → `🧩 目录-B🧹 整理（Organize）`
5. **移动后直接更新目录**：合并 Mulu-fenlei 职责，无需调用其他 Skill

## 输入：YAML Contract + Compiler 输出文件

**输入来源有两个**：
1. **Planner 的 YAML 执行计划** — 告诉 Organizer 每个文件该做什么（create/merge/link/discard）
2. **Compiler 输出的实际文件** — 在 `B🧹 整理（Organize）/` 下，是 Organizer 要移动的实际文件

**Pipeline 调用时的完整数据流**：
```
Capture 源文件
    → Planner 分析 → 输出 YAML 计划（logs/planner/*.yaml）
    → Compiler 编译 → 输出标准文件（B🧹 整理/）
    → Organizer（本 Skill）→ 读取 YAML 计划 + 读取 B🧹 整理下的实际文件 → 移动归位
```

**单独调用时**：直接处理指定目录下的文件，不需要 Planner YAML。

### Planner YAML 格式（Pipeline 调用时读取）

从 Planner 输出的 YAML 文件读取：

```yaml
版本: "1.0"
生成时间: "YYYY-MM-DD HH:MM"
来源路径: "A📥 收集（Capture）"
文件数: X
文件:
  - 文件名: "xxx.md"
    分类: 知识
    评分: 85
    标题: "Claude - 工具 @ API配置方法"
    主题:
      实体: ["Claude"]
      概念: ["API调用配置"]
    内容摘要: "Claude API的配置步骤和常见问题"
    建议页面:
      - 标题: "Claude - 工具 @ API配置方法"
        类型: 工具
        动作: create  # create | merge | link
        已有页面:  # 仅 merge/link 时填写
          路径: "1- 🤖AI 相关/AI智能体claw/Claude - 工具 @ API配置.md"
          相似度: 85
置信度: 85
```

## 输入处理流程

### Step 1：读取知识规划

读取 `logs/planner/knowledge-plan-*.yaml`，获取 YAML Contract。

### Step 2：读取知识库目录地图

读取 `📖目录 索引.md`，解析为 vault_registry：

解析规则：
1. 以 `## ` 开头的行 = 一级领域
2. 以 `### ` 开头的行 = 二级目录
3. `- [[` 开头的行 = 文件条目
4. 提取文件名和路径信息

### Step 3：目录候选匹配

根据主题/实体/类型，在 vault_registry 中寻找候选目录。

### Step 4：文件候选搜索

只搜索候选目录，读取文件名/Frontmatter/标签，寻找相关文件。

### Step 5：深度关联分析

候选文件达到阈值后读取正文，分析主题一致性、实体关系。

## 关联评分模型（0-100）

对每个候选文件计算 relation_score：

| 维度 | 分数 | 说明 |
|------|------|------|
| 标题匹配 | 0-20 | 完全包含=20, 同义词=15, 部分重叠=10 |
| 目录匹配 | 0-20 | 同一目录=20, 父子目录=15, 同级=10 |
| 实体匹配 | 0-20 | 核心实体一致=20, 部分重叠=15 |
| 正文语义 | 0-25 | 主题一致=25, 相关=18, 交叉=12 |
| 知识关系 | 0-15 | 父子依赖=15, 补充使用=10, 弱相关=5 |

### 与 Planner 相似度的关系（重要）

| 维度 | Planner 相似度 | Organizer relation_score |
|------|---------------|------------------------|
| 用途 | 判断"是否已存在同类页面"（触发 merge/link） | 判断"应归属到哪个目录、是否建立双链" |
| 时机 | 粗筛（先于 Organizer 执行） | 精排（基于 Planner 候选页面） |
| 输入 | 新文件 vs 全库已有文件 | 新文件 vs Planner 候选页面 |
| 算法 | 标题 Jaccard×0.5 + 实体一致率×0.3 + 主题词×0.2 | 五维加权（标题+目录+实体+正文+知识关系） |

**关系链**：Planner 相似度先过滤出候选页面（< 30 直接 create，不进入 Organizer 评分），Organizer 仅对候选页面做精排，避免重复评分全库。

## 文件归属决策

| 条件 | 动作 |
|------|------|
| Planner 标记 `动作: link` | **移动到已有页面所属目录** + 建立双链 + 更新索引 |
| score >= 90（强关联） | 移动到正式目录 + 建立双链 + 更新索引（直接更新，不触发 Mulu-fenlei） |
| 60 <= score < 90（中等） | 判断成熟度 → 成熟则移动，否则进入整理区 |
| score < 60（独立页面） | 移动到 `🧩 目录-B🧹 整理（Organize）` |

### 成熟知识目录判断

满足以下全部条件视为成熟：

| 条件 | 要求 |
|------|------|
| directory_exists | 目录存在 |
| file_count | >= 3 个文件 |
| has_index_entry | 📖目录 索引.md 中有入口 |

不检查 "stable_topic"，由索引入口隐式保证。

## 🧩 目录-B🧹 整理（Organize）规则

定位：已加工完成但尚未找到知识归属的位置（非垃圾箱、非临时区）。

存放：新领域知识、独立知识节点、待未来合并知识。

### _pending 目录

score < 40 的文件移动到 `_pending/`，等用户二次决策。

## 双向链接规则

只要存在关联，必须建立。统一格式：`## 🔗 相关知识`

- **新页面**追加：

```markdown
## 🔗 相关知识

- [[已有页面]]
```

- **已有页面**追加：

```markdown
## 🔗 相关知识

- [[新页面]]
```

禁止关键词链接。必须满足至少一个：同主题、上下游、补充关系、使用关系、依赖关系。

## 📖目录 索引维护（合并 Mulu-fenlei 职责）

移动完成后**直接更新索引**，无需再调用 Mulu-fenlei：

### 更新流程

```
移动文件 → 删除索引中旧行 → 添加索引中新行 → 更新 🧩 目录-xxx.md → 生成 File Summary → 移动完整性验证 → 中间态清理
```

### 索引更新规则

| 操作 | 索引处理 |
|------|----------|
| 移动文件 | 从索引中删除旧行，添加新行（带 ✍️ + 状态:已移动） |
| 新建目录 | 在索引中添加新分类区域 |
| 合并文件 | 保留已有条目，更新 ✍️ 摘要 |

### 索引格式

```markdown
## 📁 目标目录分类
- [[新文件名]] ✍️ 一句话总结 | 更新:YYYY-MM-DD | 状态:已移动
```

## 🚫 防死循环规则（与Python脚本协调，强制）

**Python脚本负责结构维护（快、不用模型），Skill负责内容加工（AI才能做）。两边有重复工作正常，但必须避免死循环。**

### 📖目录 索引.md 分区规则

**Organizer只更新"按领域分组"区域，禁止修改Python维护的区域**：

| 区域 | 管理者 | Organizer操作 |
|------|--------|--------------|
| `## 📂 目录结构树` | Python | ❌ 禁止修改 |
| `## 📊 统计摘要` | Python | ❌ 禁止修改 |
| `## 📋 按领域分组` | Skill | ✅ 在此区域添加/更新条目 |

**Organizer更新索引时**：
1. 找到 `## 📋 按领域分组` 区域
2. 在该区域内删除旧文件条目、添加新文件条目（带✍️+状态）
3. **禁止重写** `## 📂 目录结构树` 和 `## 📊 统计摘要` 区域

### 🧩 目录文件分区规则

**Organizer更新🧩目录文件时，可重写整个分类区域**：

| 区域 | 管理者 | Organizer操作 |
|------|--------|--------------|
| 整个目录文件 | Skill | ✅ 分类整理、✍️摘要、frontmatter |



### 标题修正规则

**Python的标题修正会跳过有frontmatter的文件。Organizer生成文件时**：
1. 不要加BOM
2. 有frontmatter的文件，确保frontmatter后是`# 标题`
3. 无frontmatter的文件，确保第一行是`# 标题`

### 字段说明

| 字段 | 说明 | 必填 |
|------|------|------|
| `[[文件名]]` | 双链 | ✅ |
| `✍️ 一句话总结` | 文件摘要 | ✅ |
| `更新:YYYY-MM-DD` | 最后更新时间 | ✅ |
| `状态:已整理/未整理/已移动` | 处理状态 | ✅ |

## 移动完整性验证（强制，防止假移动）

**移动操作完成后必须验证，否则视为假移动：**

| 验证项 | 通过标准 | 失败处理 |
|--------|---------|---------|
| 目标文件存在 | 目标路径下文件实际存在 | 重新执行移动 |
| 源文件已消失 | B🧹 整理（Organize）/ 中源文件不存在 | 手动删除残留 |
| 文件大小一致 | 目标文件大小 = 源文件大小（±10 字节） | 重新移动（可能截断） |
| 内容首行一致 | 目标文件首行 = 源文件首行 | 重新移动 |
| 索引已更新 | 📖目录 索引.md 中已添加新条目 | 补充索引更新 |

**假移动场景**：
- state.json 标记"已移动"，但目标路径文件不存在 → 假移动
- B🧹 整理（Organize）/ 中间态文件还在 → 移动没完成
- 目标文件大小 0 字节 → 移动中截断

## 中间态清理（强制）

**移动完成后，B🧹 整理（Organize）/ 中的中间态文件必须清理：**

| 情况 | 处理 |
|------|------|
| 中间态文件已成功移到正式目录 | 删除 B🧹 整理（Organize）/ 中的中间态文件 |
| 中间态文件是 merge 模式（保留原文件） | 不删除（merge 已合并到目标文件） |
| 中间态文件是 link 模式 | Capture 源文件已移到已有页面同目录，B🧹 整理/ 中间态文件归档到 `D📦 归档（Archive）/processed-YYYY-MM-DD/` |

**link 模式的 Capture 源文件清理**：
- Planner 标记 link 时，Capture 源文件（如 `A📥 收集（Capture）/Ponytail.md`）需移动到已有页面同目录
- 原 Capture 文件移动后，Capture 目录中不再保留
- 若 Capture 源文件内容极少（空壳），可直接归档到 `D📦 归档（Archive）/processed-YYYY-MM-DD/`

**清理验证**：每次 Organizer 运行结束，扫描 `B🧹 整理（Organize）/`，所有已处理（移动/合并/链接）的中间态文件必须已清理，否则输出警告。

## 孤立知识检测

检测没有双链/索引/分类的知识，生成 `logs/organizer/orphan-report-YYYY-MM-DD.md`。

## 重复知识检测

处理 Planner 标记的重复检测：

| 动作 | 处理 |
|------|------|
| merge | 合并内容，保留更完整版本 |
| confirm_merge | 输出确认请求，等待用户决定 |
| link | **移动 Capture 源文件到已有页面所属目录** + 建立双链关联 |
| create | 正常创建新页面 |

### link 动作详解（重要）

当 Planner 标记 `动作: link` 时，说明新文件与已有页面高度相似（相似度 60-74），需要：

1. **移动 Capture 源文件**到已有页面所在的**同一目录**（不是新建目录，不是归档）
2. **在已有页面**添加 `## 🔗 相关知识` 双链指向新文件
3. **在新文件**添加 `## 🔗 相关知识` 双链指向已有页面
4. **更新目标目录的 🧩 目录文件**（添加新文件条目）
5. **更新 📖目录 索引.md**（在对应领域添加新条目）

**示例**：
- Capture 文件：`A📥 收集（Capture）/Ponytail.md`
- Planner 标记：`动作: link`，`目标文件: 1- 🤖AI 相关/skill插件/Ponytail - Skill介绍 @ 防止过度工程化编码.md`
- 执行：移动 `Ponytail.md` → `1- 🤖AI 相关/skill插件/Ponytail.md`（同目录）
- 双链：在 `Ponytail - Skill介绍` 添加 `[[Ponytail]]`，在 `Ponytail.md` 添加 `[[Ponytail - Skill介绍 @ 防止过度工程化编码]]`

**与 create 的区别**：
- create：文件无重复，创建新目录下的新文件
- link：文件有重复，移动到已有页面**同目录**，建立双链关联

## 日志系统

所有操作记录到 `logs/organizer/organize-YYYY-MM-DD.md`：

```markdown
# Knowledge Organizer Log

日期: YYYY-MM-DD

## 文件处理
文件: [文件名]
原位置: [原路径]
目标: [目标路径]
原因: [移动原因]
关联评分: [分数]

## 双链
新增: [[关联页面]]

## 索引更新
- 删除旧行: [[旧文件名]]
- 添加新行: [[新文件名]] ✍️ 一句话总结 | 更新:YYYY-MM-DD | 状态:已移动

## 目录更新
更新: 🧩 目录-xxx.md（合并 Mulu-fenlei 职责）
```

## 回滚机制

每次移动操作必须生成可执行的 PowerShell 回滚脚本：

```yaml
操作:
  id: "op-YYYYMMDD-001"
  类型: move
  移动前:
    路径: "B🧹 整理（Organize）/xxx.md"
  移动后:
    路径: "1- 🤖AI 相关/xxx.md"
  回滚脚本: |
    mv "1- 🤖AI 相关/xxx.md" "B🧹 整理（Organize）/xxx.md"
  撤销链接:
    - 文件: "1- 🤖AI 相关/related.md"
      移除: "- [[xxx]]"
```

## 风险等级与确认（强制执行）

### 自动风险判断规则

| 操作类型 | 风险等级 | 判断依据 |
|----------|----------|----------|
| 同目录内移动 | low | 源目录 == 目标目录 |
| 移动到已有目录 | low | 目标目录已存在 |
| 移动到新目录 | medium | 目标目录不存在，需新建 |
| 合并文件 | medium | 两个文件内容合并 |
| 批量移动 (>=5文件) | high | 一次操作多个文件 |
| 删除链接 | high | 移除双向链接 |

### 确认规则

| 等级 | 操作 | 要求 |
|------|------|------|
| low | 移动到已有目录 | 自动执行 |
| low | 建立双链 | 自动执行 |
| low | 更新索引 | 自动执行 |
| **medium** | 合并文件 | **必须用户确认** |
| **medium** | 移动到新目录 | **必须用户确认** |
| **high** | 批量移动 (>=5文件) | **必须用户确认** |
| **high** | 删除链接 | **必须用户确认** |

**medium/high 操作必须暂停，等待用户确认后才执行。**

### 内容备份机制

移动文件前，自动备份原始内容：

```yaml
备份:
  原文件: "B🧹 整理（Organize）/xxx.md"
  备份位置: "logs/organizer/backups/xxx-YYYY-MM-DD.md"
  备份内容: "文件完整内容"
```

## 三种运行模式

| 模式 | 说明 |
|------|------|
| Analyze Mode | 只分析，输出 organization-plan.md |
| Review Mode | 生成待确认操作列表（默认） |
| Execute Mode | 执行移动、链接、索引、日志 |

默认 Review Mode：分析 → 生成组织计划 → 确认 → 执行。

## 安全规则

- 默认 Review Mode，不自动执行
- 禁止：删除文件、覆盖内容、删除链接、大规模移动（>=5文件需确认）
- 处理过的文件移动到 `_trash/`，不删除

## 操作记录

所有操作必须记录到 `⚓新增文件记录.md`，格式如下：

```markdown
### YYYY年M月D日
| 操作  | 原文件名                  | 优化名称                         | 文件地址                    |
| --- | --------------------- | ---------------------------- | ----------------------- |
| 新增  | 未命名 1.md、未命名 2.md（融合） | [[OmniRoute - 工具 @ AI编程聚合网关方案]] | `1- 🤖AI 相关\AI智能体claw\` |
| 合并  | 未命名.md                | [[免费大模型API平台 - 合集 @ 5平台对比与调用教程]] | `1- 🤖AI 相关\AI智能体claw\` |
| 删除  | 未命名 1.md              | -                            | `_trash/`              |
| 归档  | Groq.md                 | [[Groq - 极速推理API @ 代码生成首选平台]] | `D📦 归档（Archive）/processed-YYYY-MM-DD/` |
```

**操作类型说明**：

| 操作 | 含义 | 源文件去向 |
|------|------|-----------|
| 新增 | 创建新文件 | 原文件 → `D📦 归档（Archive）/` |
| 合并 | 合并到已有文件 | 原文件 → `D📦 归档（Archive）/` 或 `_trash/` |
| 归档 | 已有对应文档，直接归档 | 原文件 → `D📦 归档（Archive）/` |
| 删除 | 无意义/空文件 | 原文件 → `_trash/` |

追加写入，不覆盖已有记录。
日期降序排序，最新的显示在最上面
---

## 状态追踪机制

### 追踪文件位置

```
logs/organizer/organizer-state.json
```

### 追踪数据结构

```json
{
  "版本": "1.0",
  "上次运行": "ISO 8601 时间戳",
  "文件状态": {
    "文件路径": {
      "原路径": "B🧹 整理（Organize）/文件.md",
      "目标路径": "1- 🤖AI 相关/文件.md",
      "处理状态": "已移动 | 已跳过 | 已建链",
      "处理时间": "最后处理时间",
      "风险等级": "low | medium | high",
      "回滚脚本": "mv 命令"
    }
  }
}
```

### 变化检测规则

| 条件 | 判断 | 动作 |
|------|------|------|
| 文件已在目标目录 | 未变化 | 跳过 |
| 文件不在追踪记录中 | 新增 | 处理 |
| 文件在追踪中但路径已变 | 修改 | 处理 |
| 文件在追踪中且路径一致 | 未变化 | 跳过 |

### 执行后必须

1. 更新 `logs/organizer/organizer-state.json`
2. 写入运行日志 `logs/organizer/organizer-YYYY-MM-DD.md`
3. **更新 `📖目录 索引.md` 中对应条目（删除旧行 + 添加新行）**


---

## 2. SKILL.md

---
name: obsidian-knowledge-planner
description: 分析 Obsidian Capture 碎片笔记，识别知识价值，拆分主题，设计原子知识页面，输出结构化执行计划。触发词：knowledge plan, 分析笔记, 规划知识, 知识拆分, 内容分析.
---

# Obsidian Knowledge Planner

认知规划引擎：理解 → 判断 → 规划 → 输出执行方案。不生成内容、不命名、不移动文件。

上游：无（入口） / `obsidian-pipeline`（调度器调用时）
下游：`obsidian-knowledge-compiler`（生成标准文档+正确文件名）→ `obsidian-knowledge-organizer`（目录移动+双链+目录更新）

## 双层架构

| 层级 | 文件 | 用途 | 写入者 |
|------|------|------|--------|
| 人类可读层 | `📖目录 索引.md` | 快速判断"哪些整理了/没整理" | Mulu-fenlei + Organizer |
| 机器可读层 | `logs/planner/planner-state.json` | 记录标题、哈希、分析结果、处理状态 | Planner |

**读取优先级**：
1. 先读索引（快速跳过已处理文件）
2. 再读状态文件（精确检测标题变化）

## 来源路径限制

**只处理 `A📥 收集（Capture）` 文件夹下的文件。**

- 默认扫描 `A📥 收集（Capture）/` 及其子文件夹
- 若用户明确指定其他路径（如 `B🧹 整理（Organize）`），需在输出中注明来源路径
- 非 Capture 路径的文件，输出时增加 `source_override: true` 标记

## 工作流程

```
两种入口 → 快速扫描（Pipeline传递/自行扫描） → 哈希去重 → 跳过判断 → 价值评分 → 知识建模 → 已有页面检测 → 主题拆分 → 页面设计 → 输出 Plan
```

## 阶段0：快速扫描（节省Token）

**两种入口：**

| 入口 | 说明 | 扫描方式 |
|------|------|----------|
| **Pipeline 调用** | 接收 Pipeline 的扫描结果，无需再次扫描 | Pipeline 传递标题+哈希+前20行 |
| **单独调用** | 用户直接调用 Planner 分析某文件夹 | 自行扫描前 20 行 + 计算 MD5 哈希 |

### 输入

若由 Pipeline 调用，扫描结果包含：
- 文件路径
- 文件标题（第一个 # 开头的行）
- 文件哈希（MD5）
- 变化检测结果（新增/修改/删除/未变化）

若单独调用，Planner 自行完成上述扫描。

### 判断规则

| 条件 | 判断 | 动作 |
|------|------|------|
| 文件大小 < 10 字节，文件名可推断明确标题 | 空壳文件（有标题可推断） | **内容补充模式**：传给 Compiler 基于标题搜索补充内容 |
| 文件大小 < 10 字节，标题含"未命名"或无意义 | 空壳文件（无意义标题） | 跳过（标记 discard） |
| 文件大小 < 10 字节，有内容（如"Trae"） | 非空壳 | 正常进入深度分析 |
| 哈希值在状态记录中存在 | 重复文件 | 跳过（标记为 `discard`） |
| 头部内容全是图片链接或乱码 | 无效内容 | 跳过 |
| 头部内容有明确主题 | 有效内容 | 进入深度分析 |

**Pipeline 已提取标题并存入状态，用于变更检测。**

### 哈希去重机制

```json
{
  "哈希记录": {
    "md5哈希值": {
      "首次处理": "ISO 8601 时间戳",
      "文件路径": "A📥 收集（Capture）/文件.md",
      "处理状态": "已处理 | 已归档 | 已丢弃"
    }
  }
}
```

**如果两个文件 MD5 哈希相同，第二个文件直接标记为 `discard`（重复），不进入后续流程。**

## 阶段1：文件跳过判断

以下文件直接跳过，不进入评分：

| 条件 | 处理 |
|------|------|
| 文件名以 `🧩 目录-` 开头 | 跳过 |
| 文件名以 `🏠 home-` 开头 | 跳过 |
| 文件名为 `README.md` / `CLAUDE.md` / `AGENTS.md` | 跳过 |
| 文件大小 < 10 字节 | 跳过 |
| 文件修改时间 > 180天 且 无标签 | 归档 |

## 阶段2：内容价值分类

读取文件后，按以下分类归类：

| 分类 | 特点 | 处理 |
|------|------|------|
| 知识 | 可复用、可学习、可迁移 | 进入评分 |
| 参考 | 有价值但暂无需加工 | 标记保存，不进入编译 |
| 想法 | 未来可能产生产品/项目/内容 | 标记保存 |
| 收件箱 | 无法判断（截图、一句话记录） | 标记保存 |
| 归档 | 历史信息 | 标记归档 |
| 丢弃 | 广告、重复、无意义 | 移动到 `_trash/` |

**分类字段统一中文**：禁止使用英文（knowledge/reference/idea 等仅作历史参考）。

### discard 处理规则

标记为 丢弃 的文件，执行：
1. 移动到 `_trash/` 目录（不存在则创建）
2. 在 YAML 输出中记录 `丢弃原因`
3. 禁止直接删除文件

## 阶段3：价值评分（0-100）

计算 Knowledge Value Score：

```
价值评分 = 知识价值(0-50) + 完整性(0-50)
```

| 维度 | 评分标准 |
|------|----------|
| 知识价值(0-50) | 50=核心方法论, 35=可复用技巧, 20=一般参考, 0=临时信息 |
| 完整性(0-50) | 50=结构完整+有案例, 35=有核心内容, 20=片段信息, 0=无法理解 |

| 总分 | 动作 |
|------|------|
| 70-100 | 立即规划（进入编译） |
| 40-69 | 保存参考（不编译，留在 Capture） |
| 0-39 | 归档或 discard |

### ⭐ 空壳文件特殊规则（内容补充模式）

**定义**：文件只有标题（或标题+极少内容，正文 < 100 字符），但标题明确指向一个具体的工具/概念/知识点。

**判断条件**（同时满足）：
1. 文件大小 < 10 字节，或正文内容 < 100 字符（去除标题和空行后）
2. 文件名可推断出明确标题（含具体工具名、软件名、概念名等）
3. 不是完全无意义的标题（如"未命名"、"新建文档"等）

**处理方式**：
| 情况 | 动作 | 示例 |
|------|------|------|
| 空文件，文件名有明确标题 | **内容补充模式** → 传给 Compiler 搜索补充 | `OmniRoute 不太行 太容易触发风控了.md` |
| 空文件，标题含"未命名" | 跳过（discard） | `未命名 1.md` |
| 有内容（即使很小），标题含"未命名" | 正常处理，按内容分析 | `未命名.md` 内容"Trae" → 正常分析 |

**输出标记**：
- 动作：`create`
- 模式：`模式: 内容补充`
- 说明：传给 Compiler，由 Compiler 基于标题搜索相关信息补充完整内容

**示例**：
- ✅ 内容补充：`ShareX 截图、文件共享和生产力工具.md`（空文件但文件名明确）→ create（内容补充）
- ✅ 内容补充：`OmniRoute 不太行 太容易触发风控了.md`（空文件但文件名明确）→ create（内容补充）
- ❌ 跳过：`未命名 1.md`（标题无意义）→ discard
- ✅ 正常处理：`未命名.md` 内容为"Trae" → 正常分析

## 阶段4：知识建模

提取核心要素：

- **实体**：具体对象（工具名、人名、产品名）
- **主题**：核心概念（方法论、原理、流程）

## 阶段5：已有页面检测（强制）

**每个建议页面必须检测是否已有对应页面，避免重复创建。**

### 检测方法

```bash
# 提取新文件的标题关键词
新文件标题 = "# 标题" 的核心词（如工具名、主题名）

# 搜索已有文件
rg -l "关键词" "D:\Obsidian\LeoDiary" --include "*.md" -g "!_*" -g "!logs/" -g "!🧩*" -g "!🏠*"
```

### 判断规则（简化版）

**相似度算法（必须按公式计算，禁止主观判断）：**

```
相似度 = 标题Jaccard重叠度 × 0.5 + 核心实体一致率 × 0.3 + 主题词匹配率 × 0.2
```

| 维度 | 权重 | 计算方式 |
|------|------|----------|
| 标题 Jaccard 重叠度 | 0.5 | `交集词数 / 并集词数 × 100`（按词切分） |
| 核心实体一致率 | 0.3 | `一致实体数 / 新文件实体数 × 100` |
| 主题词匹配率 | 0.2 | `匹配主题词数 / 新文件主题词数 × 100` |

**示例：**
- 新文件标题 `Claude - 配置 @ API连接方法`，已有 `Claude - 教程 @ API配置`
- 标题词交集：{Claude, API}，并集：{Claude, 配置, API, 连接方法, 教程} → Jaccard = 2/5 = 40
- 核心实体一致：Claude → 1/1 = 100
- 主题词匹配：API配置 → 1/1 = 100
- 相似度 = 40×0.5 + 100×0.3 + 100×0.2 = 20 + 30 + 20 = **70** → merge/link

| 相似度 | 动作 | 说明 |
|--------|------|------|
| >= 75 | merge | 高度相似，合并到已有文件 |
| 60-74 | link | 中度相似，建立双链 |
| 30-59 | link | 弱相似，建立双链（附关联理由） |
| < 30 | create | 无重复，创建新文件 |

### 已有页面信息提取

若检测到已有页面，提取以下信息供 Compiler 使用：

```yaml
已有页面:
  路径: "1- 🤖AI 相关/AI智能体claw/xxx.md"
  标题: "xxx - 教程 @ 说明"
  相似度: 85
```

## 阶段6：主题边界检测

按语义拆分（非标题），满足以下 2 项以上即拆：

- 主题对象不同
- 目标不同
- 用户问题不同
- 知识领域不同
- 使用场景不同

### 粒度控制示例

| 情况 | 决策 |
|------|------|
| Python基础/变量/函数/类 | 不拆（同一主题的子概念） |
| Python教程 + Docker教程 | 拆开（不同主题） |
| Claude API配置 + Claude使用技巧 | 不拆（同一工具） |
| Claude API + ChatGPT API | 拆开（不同工具） |

## 阶段7：知识页面设计

设计未来应有哪些页面（不直接生成文件）：

```yaml
pages:
  - title: "工具名 - 类型 @ 简短说明"
    类型: 教程 | 概念 | 参考 | 故障排查 | 经验 | 清单 | 合集
    目的: "解释核心概念"
    来源范围: "原始文件 §1-3"
```

**类型字段统一中文**：禁止使用英文（tutorial/concept 等仅作历史参考）。

## 输出标准（强制双格式）

### YAML Contract 版本协商（强制）

**所有输出的 YAML 必须在首字段声明 `版本`，下游 Skill（Compiler/Organizer）必须先校验版本再消费。**

| 版本 | 兼容性 | 处理 |
|------|--------|------|
| 1.0 | 当前版本 | 正常消费 |
| 2.0+ | 不兼容 | 拒绝消费，报错"YAML contract 版本不兼容，需要 Planner 升级"或"Compiler/Organizer 升级" |
| 缺失 | 不合规 | 拒绝消费，报错"YAML contract 缺少版本字段" |

**版本变更规则**：
- 字段重命名（如 `knowledge` → `知识`）→ 主版本号 +1
- 新增可选字段 → 次版本号 +1（兼容）
- 字段含义变化 → 主版本号 +1

下游 Skill 在读取 YAML 时必须执行：

```python
if yml.get("版本") != "1.0":
    raise ValueError(f"YAML contract 版本不兼容: {yml.get('版本')}, 期望 1.0")
```

### 双格式输出（强制）

**必须同时输出两个文件：**

### 1. 人类可读：`logs/planner/knowledge-plan-YYYY-MM-DD.md`

```markdown
# Knowledge Plan

## 概览
- 文件数: X
- 立即规划: X
- 保存参考: X
- 归档/discard: X

## 文件分析

### [文件名]
- 分类: 知识
- 评分: 85
- 主题: xxx
- 建议页面:
  1. xxx - 教程 - xxx
```

### 2. 机器可读：`logs/planner/knowledge-plan-YYYY-MM-DD.yaml`

```yaml
版本: "1.0"
生成时间: "YYYY-MM-DD HH:MM"
来源路径: "A📥 收集（Capture）"
文件数: X

文件:
  - 文件名: "xxx.md"
    分类: 知识
    评分: 85
    标题: "Claude - 工具 @ API配置方法"
    主题:
      实体: ["Claude"]
      概念: ["API调用配置"]
    内容摘要: "Claude API的配置步骤和常见问题"
    建议页面:
      - 标题: "Claude - 工具 @ API配置方法"
        类型: 工具
        动作: create  # create | merge | link
        已有页面:  # 仅 merge/link 时填写
          路径: "1- 🤖AI 相关/AI智能体claw/Claude - 工具 @ API配置.md"
          相似度: 85

  - 文件名: "广告.md"
    分类: 丢弃
    评分: 0
    丢弃原因: "推广内容"
    处理: "移动到 _trash/"

置信度: 85
```

## 置信度系统

| 置信度 | 行为 |
|--------|------|
| 80-100 | 自动进入编译 |
| 60-79 | 建议编译，等待确认 |
| < 60 | 保持原状 |

## 安全规则

- **禁止**：删除文件、覆盖原文件、修改原内容
- **定位**：Planner 是分析器，只输出执行计划，不执行实际文件操作
- **默认**：PLAN ONLY（分析 → 输出计划 → 等待确认）
- **关于 discard**：Planner 只标记"建议丢弃"并输出建议动作，实际移动到 `_trash/` 的操作由 Pipeline（或用户确认后）执行
- **关于移动**：Planner 不执行任何文件移动操作，所有移动操作由 Pipeline / Organizer / Fire-rename 执行

## 操作记录

所有操作必须记录到 `⚓新增文件记录.md`，格式如下：

```markdown
### YYYY年M月D日
- 【回收】文件名 D:\Obsidian\LeoDiary\_trash\文件名（原因：discard）
```

**注意**：discard 文件记为「回收」（移到 _trash/），不是「删除」。Planner 只负责记录建议，实际操作由 Pipeline 执行。

---

## 状态追踪机制

### 追踪文件位置

```
logs/planner/planner-state.json
```

### 追踪数据结构

```json
{
  "版本": "1.0",
  "上次运行": "ISO 8601 时间戳",
  "文件状态": {
    "A📥 收集（Capture）/文件名.md": {
      "路径": "相对路径",
      "标题": "文件的第一个 # 标题",
      "处理状态": "已处理 | 已跳过 | 已归档 | 已丢弃",
      "处理时间": "最后处理时间",
      "分析结果": {
        "分类": "知识",
        "评分": 85,
        "动作": "create | merge | link",
        "目标文件": "merge/link 时的目标文件路径"
      }
    }
  },
  "哈希记录": {
    "md5哈希值": {
      "文件路径": "A📥 收集（Capture）/文件.md",
      "处理状态": "已处理 | 已归档 | 已丢弃"
    }
  }
}
```

### 变化检测规则

**核心逻辑：标题变了 = 文件身份变了 = 必须处理**

| 优先级 | 条件 | 判断 | 动作 |
|--------|------|------|------|
| 1 | 文件不在追踪记录中 | 新增 | 处理 |
| 2 | 文件的 # 标题与记录不同 | 修改（身份变了） | 处理 |
| 3 | 文件在追踪中但不存在 | 删除 | 标记删除 |
| 4 | 以上全都不满足 | 未变化 | 跳过 |

**说明**：
- 内容修改但标题不变 → 跳过 Planner（Pipeline 会触发轻量路径，仅调用 Mulu-fenlei-summary 重生成 File Summary）
- 标题修改 → 处理（文件身份变了，需要重新评估）
- MD5 哈希仅用于去重（检测两个文件是否内容相同），不用于 Planner 变更检测

### 执行后必须

1. 更新 `logs/planner/planner-state.json`
2. 写入运行日志 `logs/planner/planner-YYYY-MM-DD.md`


---

## 3. SKILL.md

---
name: obsidian-mulu-fenlei-summary
description: 整理 Obsidian 目录文件并为每个文件生成 ✍️ File Summary（文件索引摘要）。触发词：分类目录、目录分类、重写目录、文件摘要、生成摘要、File Summary、摘要目录 xxx、目录摘要。独立使用，用于批量整理目录文件。注意："整理 xxx 目录"（如"整理 AI 目录"）触发 Organizer，"摘要目录 xxx"或"分类目录 xxx"触发本 Skill，避免歧义。
---

# Obsidian 目录分类与文件摘要管理

**检查补漏引擎**：扫描目录 → 检查 ✍️ 摘要完整性 → 补充缺失摘要 → 同步目录文件 + 索引。

**核心定位**：轻量化检查工具，**不做全量重生成**。文件内部的 `>✍️` 摘要由 Compiler 在生成文件时就写好，本 Skill 只负责查漏补缺。

**独立工具**：用户可单独调用处理任何目录文件夹。不在 Pipeline 主流程中。

**例外**：Pipeline 检测到"标题不变+内容变化"时，会触发本 Skill 重生成 File Summary（轻量路径）。

上游：无（独立工具） / `obsidian-pipeline`（轻量路径触发时） / `obsidian-fire-rename`（改名后同步更新目录/索引中的 ✍️ 条目）
下游：无（最终输出）

## 与相关 Skill 的职责边界

| 场景 | 用哪个 | 为什么 |
|------|--------|--------|
| 目录文件分类 + ✍️ 摘要补漏 | **Mulu-fenlei-summary（本 Skill）** | 纯目录整理，不移动文件 |
| 移动文件 + 建双链 + 更新目录 | **Organizer** | 知识归位整理，涉及文件物理移动 |
| 批量重命名 + 双链维护 | **Fire-rename** | 改名不改内容 |

**简单判断**：文件要动位置 → Organizer；目录要整理/摘要要补 → Mulu-fenlei-summary；名字要改 → Fire-rename。

## 触发词去歧义（重要）

| 触发词 | 触发的 Skill | 说明 |
|--------|--------------|------|
| "摘要目录 xxx" / "目录摘要" / "分类目录 xxx" / "文件摘要" | **本 Skill** (Mulu-fenlei-summary) | 生成 ✍️ File Summary + 目录分类 |
| "整理 xxx 目录" / "整理 AI 目录" / "整理知识" | **Organizer** | 归属目录 + 移动文件 + 建立双链 |
| "重命名 xxx" / "批量重命名" | **Fire-rename** | 规范化文件名 + 双链维护 |
| "处理 Capture" / "运行流水线" | **Pipeline** | 调度器，调用 Planner → Compiler → Organizer |

**禁止使用模糊触发词**："整理目录文件"（已弃用，容易与 Organizer 混淆），改用"摘要目录 xxx"。

## 双层架构

| 层级 | 文件 | 用途 | 写入者 |
|------|------|------|--------|
| 人类可读层 | `📖目录 索引.md` + `🧩 目录-xxx.md` | 浏览导航、File Summary 展示 | Mulu-fenlei-summary（直接写入 ✍️ 摘要） |
| 机器可读层 | `logs/summary/summary-state.json` | 记录目录状态、文件数、摘要数 | Mulu-fenlei-summary |

**读取优先级**：
1. 先读索引（快速判断哪些已有摘要）
2. 再读状态文件（精确检测目录变化）

## 执行工具

- Read、Write、Edit、Glob、Grep

---

## ⚠️ 强制执行清单（必须全部完成）

**每次执行本 Skill，必须按顺序完成以下 4 步，缺一不可。**

### Step 1：文件内部摘要（源文件）— 详细版

| 项目 | 说明 |
|------|------|
| **操作对象** | 目录中的每个 `.md` 源文件 |
| **操作位置** | `# 标题` 下方，格式 `>✍️ 一句话总结` |
| **格式** | `>✍️ + 空格 + 一句话（50-100字，可包含模块、特点、价值等）` |
| **生成者** | Compiler（新建文件时） / Mulu-fenlei-summary（补漏时） |
| **检查机制** | 读前 10 行找 `>✍️` 行 → 有则进入质量判定 → 优质则跳过，低质则重写；无 ✍️ 则读取正文生成 |
| **质量判定** | 见下方「保护规则的边界」 |
| **改名处理** | 文件名变了但 ✍️ 内容仍匹配 → 保留不重生成；内容不匹配 → 才更新 |

**轻量化原则**：约 90% 的文件已有 ✍️ 且质量合格，直接跳过。只处理缺失或低质的少数文件。

### Step 2：目录索引摘要（目录文件）— 精简版

| 项目 | 说明 |
|------|------|
| **操作对象** | `🧩 目录-xxx.md` 文件 |
| **操作位置** | 每个 `[[链接]]` 之后 |
| **格式** | `[[文件名]] ✍️ 一句话总结` |
| **长度** | **≤30字**，只抓最核心要点（是什么 + 核心价值） |
| **生成来源** | 从源文件 `>✍️` 详细版提炼精简，不读完整正文 |
| **检查机制** | 链接后无 ✍️ → 补充；有 ✍️ → 进入质量判定 → 优质则保留，低质则重写 |
| **质量判定** | 同 Step 1：仅标题改写/通用词 → 重写；包含具体关键词 → 保留 |

**双版本区别**：
- 源文件 `>✍️` = 详细版（50-100字，打开文件看完整信息）
- 目录 `✍️` = 精简版（≤30字，快速浏览抓核心）

### Step 3：索引更新（同步写入 📖目录 索引.md）

| 项目 | 说明 |
|------|------|
| **操作对象** | `📖目录 索引.md` 中对应分类区域 |
| **操作位置** | 找到对应分类 → 追加新条目 / 更新已存在条目 |
| **格式** | `[[文件名]] ✍️ 一句话总结 \| 更新:YYYY-MM-DD \| 状态:已整理` |
| **检查机制** | 文件不存在 → 追加；已存在无 ✍️ → 更新；已有 ✍️ → 跳过（保护规则） |

详细写入规则见下方"索引写入规则"章节。

### Step 4：完成验证（必须报告）

**执行完毕后，必须输出以下验证报告：**

```markdown
## ✍️ File Summary 执行报告

### Step 1：文件内部摘要
- 扫描文件数：X
- 已有 ✍️：X（跳过）
- 新增 ✍️：X

### Step 2：目录索引摘要
- 处理目录数：X
- 已有 ✍️：X（跳过）
- 新增 ✍️：X

### Step 3：遗漏检查
- 缺少 ✍️ 的源文件：[列出文件名]
- 缺少 ✍️ 的目录条目：[列出文件名]

### Step 4：索引更新
- 更新条目数：X
- 新增条目：X
- 更新条目：X
```

**禁止只做 Step 2 跳过 Step 1。禁止跳过 Step 3 索引更新。禁止跳过 Step 4 验证。**

---

## 工作流程

```
Step 1: 遍历目录源文件 → 快速检查 H1 下方是否有 >✍️ → 有则跳过（90%文件）→ 无才读正文生成
Step 2: 更新目录文件 → 每个[[链接]]后补充 ✍️ 摘要（有则跳过，无则补充）
Step 3: 更新索引 → 将整理结果写入 📖目录 索引.md（✍️ + 更新时间 + 状态）
Step 4: 输出执行报告 → 列出扫描数、跳过数、新增数、遗漏数
```

**性能优化**：Step 1 只读前 10 行判断有无 ✍️，有则直接跳过不读正文，大幅降低 token 消耗。

---

## File Summary 规范

### 定义

File Summary = 文件索引摘要，用于人和 AI 快速理解文件价值。

**不是**：文件备注、简短描述、标签、标题重复
**是**：一句话总结（核心格式）

### 两种格式

| 格式 | 位置 | 长度 | 用途 |
|------|------|------|------|
| **详细版** | 源文件内 `>✍️`（引用块，H1 下方） | 50-100字 | 打开文件看完整信息 |
| **精简版** | 目录文件 `[[链接]] ✍️`（内联） | ≤30字 | 快速浏览抓核心 |

### 详细版生成公式（源文件内 `>✍️`）

```
>✍️ 这是什么 + 解决什么问题 + 核心特点/价值（50-100字，一句话，引用块格式）
```

**示例**：

| 文件名 | 详细版 ✍️ |
|--------|----------|
| 再审申请书 - 定稿 @ 上级法院正式版 | `>✍️ 上级法院提交的再审申请书正式版本，包含再审请求、事实认定与法律依据，用于启动审判监督程序` |
| Docker - 工具 @ 容器部署方法 | `>✍️ Docker 容器化部署完整教程，从零搭建环境到生产部署，含安装步骤、常用命令和 Compose 配置` |
| AutoHotkey - 脚本 @ 个人常用 | `>✍️ AutoHotkey v1 个人完整版脚本，包含热字符串、日期插入、窗口控制、剪贴板增强等常用功能` |

### 精简版生成公式（目录内 `✍️`）

```
[[文件名]] ✍️ 是什么 + 核心价值（≤30字，一句话，内联格式）
```

**示例**：

| 文件名 | 精简版 ✍️ |
|--------|----------|
| 再审申请书 - 定稿 @ 上级法院正式版 | `[[再审申请书 - 定稿 @ 上级法院正式版]] ✍️ 再审申请正式版本` |
| Docker - 工具 @ 容器部署方法 | `[[Docker - 工具 @ 容器部署方法]] ✍️ Docker 容器化部署教程` |

### 质量标准

| 检查项 | 要求 |
|--------|------|
| 一句话总结 | ✅ 必填，说明文件是什么 |
| 核心要点 | ✅ 3-5个，说明文件包含什么 |
| 关联文件 | ✅ 可选，关联其他知识页面 |
| 是否超过一句空话 | ✅ |
| 是否方便 AI 判断 | ✅ |

不合格示例：`✍️ 资料`（无检索价值，拒绝）

---

## 🚫 反偷懒硬约束（强制，违反即重写）

以下 5 条硬约束，任何一条违反都视为"假执行"，必须立即重写摘要。

### 约束 1：禁止照搬标题

| 情况 | 判定 |
|------|------|
| ✍️ 内容 = 标题原文 | ❌ 假执行 |
| ✍️ 内容 = 标题去掉 `@` / `-` / `#` 等标点 | ❌ 假执行 |
| ✍️ 内容 = 标题的同义改写但无新增信息 | ❌ 假执行 |
| ✍️ 内容包含标题但额外说明了用途/内容 | ✅ 通过 |

**示例**：

| 文件标题 | ✍️ 内容 | 判定 |
|---------|---------|------|
| AutoHotkey - 脚本源码 @ 个人完整版 | `AutoHotkey - 脚本源码 @ 个人完整版` | ❌ 照搬 |
| AutoHotkey - 脚本源码 @ 个人完整版 | `AutoHotkey个人完整版脚本源码` | ❌ 仅去掉标点 |
| AutoHotkey - 脚本源码 @ 个人完整版 | `AutoHotkey个人完整版脚本源码，包含文本热字符串、日期插入、窗口控制等` | ✅ 包含具体内容 |

### 约束 2：强制读取正文

| 文件大小 | 最少读取行数 |
|---------|-------------|
| < 50 行 | 全文读取 |
| 50-500 行 | 至少前 50 行 + 章节标题扫描 |
| > 500 行 | 至少前 100 行 + 全部 `##` / `###` 章节标题 |

**禁止**：仅看标题就生成摘要。

### 约束 3：最小信息量约束

✍️ 内容必须包含以下 3 要素中的至少 2 个：

| 要素 | 说明 | 示例 |
|------|------|------|
| 文件是什么 | 类别/性质 | `脚本源码` / `教程` / `配置文件` |
| 文件用途 | 解决什么问题 | `用于自动化 Windows 操作` |
| 核心内容 | 正文中的具体关键词（≥2 个） | `热字符串` / `窗口控制` / `日期插入` |

**不合格**：`✍️ AutoHotkey 资料`（仅 1 要素，无具体关键词）
**合格**：`✍️ AutoHotkey v1 个人完整版脚本，包含热字符串、日期插入、窗口控制、剪贴板增强等常用功能`（3 要素齐全 + 4 个具体关键词）

### 约束 4：保护规则的边界（重要修正）

**保护规则不保护低质摘要**。优先级修正为：

```
人工摘要（含具体内容） > AI 优质摘要（含具体内容） > AI 低质摘要（照搬标题/无信息量）
```

| ✍️ 已存在内容 | 判定 | 处理 |
|--------------|------|------|
| 包含 ≥2 个正文具体关键词 | 优质 | 跳过（保护） |
| 仅是标题改写 / 通用词（"资料"/"文档"/"相关"） | 低质 | **强制重写**（不受保护规则约束） |
| 与标题完全相同 | 假执行 | **强制重写** |

**判定方法**：扫描 ✍️ 内容，提取所有名词/动词关键词，与文件正文前 50 行做匹配。匹配数 < 2 → 低质摘要。

### 约束 5：Step 4 验证清单升级

原 Step 4 只检查"✍️ 是否存在"，现升级为 5 项质量检查：

```markdown
## ✍️ File Summary 执行报告

### Step 1：文件内部摘要
- 扫描文件数：X
- 已有 ✍️：X（优质，跳过）
- 新增 ✍️：X
- 重写 ✍️：X（低质摘要修复）

### Step 2：目录索引摘要
- 处理目录数：X
- 已有 ✍️：X（优质，跳过）
- 新增 ✍️：X
- 重写 ✍️：X（低质摘要修复）

### Step 3：质量验证（新增）
- 照搬标题检查：[通过/失败 + 文件列表]
- 信息量检查：[通过/失败 + 文件列表]
- 正文关键词匹配：[通过/失败 + 文件列表]

### Step 4：遗漏检查
- 缺少 ✍️ 的源文件：[列表]
- 缺少 ✍️ 的目录条目：[列表]

### Step 5：索引更新
- 更新条目数：X
```

**禁止跳过 Step 3 质量验证**。质量验证失败的文件必须当场重写，不能"等下次运行"。

---

## 文件内部摘要格式

所有正式 Markdown 文件，标题后第二行必须存在：

```markdown
# 文件标题

>✍️ 文件索引摘要

---

正文内容
```

### 检查机制

| 情况 | 处理 |
|------|------|
| 不存在 ✍️ | 读取正文 → 生成摘要 → 写入第二行 |
| 已存在 ✍️ | 跳过，不覆盖 |

---

## 目录索引摘要格式

目录文件中每个条目必须包含 ✍️：

```markdown
## 📁 分类名称
- [[文件名称]] ✍️ 文件索引摘要
- [[另一个文件]] ✍️ 文件索引摘要
```

### 检查机制

| 情况 | 处理 |
|------|------|
| 链接后无 ✍️ | 读取文件 → 生成摘要 → 补充 |
| 链接后有 ✍️ | 保留，不覆盖 |

---

## 摘要保护规则

**已存在的 File Summary 默认不可覆盖。**

优先级：

```
人工摘要 > AI生成摘要 > 自动推测摘要
```

禁止 AI 每次运行重新改写旧摘要，避免产生漂移。

---

## 目录分类规则

### 智能分类强制规则（最高优先级）

**每次整理目录文件时，必须基于文件名智能分类，禁止出现"未分类"分类。**

| 规则 | 说明 |
|------|------|
| **禁止"未分类"** | 目录中不得出现 `##### 📦 未分类` 或类似分类，所有文件必须归入有意义的分类 |
| **基于文件名分类** | 读取目录中所有文件名，分析文件名中的关键词（工具名、功能词、类型词），自动推断分类 |
| **多维度分类** | 可按功能（安装/内容生产/浏览器/开发工具）、按平台（Codex/Claude/GitHub）、按类型（教程/参考/配置）等多维度分类 |
| **少于3条可不分类** | 目录中文件数 < 3 时，可保持不分类（但若文件能归类，仍建议分类） |
| **分类数量合理** | 分类数 = 文件数 ÷ 3~5（向上取整），最少2个分类，最多8个分类 |

### 分类推断流程

```
1. 遍历所有文件名 → 提取关键词（工具名、功能词、类型词）
2. 按关键词聚类 → 找出共同主题
3. 选择分类维度 → 功能/平台/类型/场景
4. 生成分类名称 → emoji + 简洁中文（如"🔧 安装管理"、"📝 内容生产"）
5. 分配文件到分类 → 每个文件必须归属一个分类
```

### 格式规则

```markdown
---
title: 🧩 目录-xxx
类型: 目录
标签: [目录]
---

# 🧩 目录-xxx

## 📌 一句话总结
{一句话说明这个目录包含什么内容}

## 📁 {分类1名称}
- [[文件1]] ✍️ 摘要内容
- [[文件2]] ✍️ 摘要内容

## 📁 {分类2名称}
- [[文件3]] ✍️ 摘要内容

[[🏠 home-xxx]]
```

### 分类维度优先级

1. **功能/用途**（最高优先级）：文件解决什么问题 → 安装管理、内容生产、浏览器自动化、开发工具
2. **平台/工具**：文件针对什么平台 → Codex相关、Claude相关、GitHub相关
3. **内容类型**：文件是什么类型 → 教程、参考、配置、清单
4. **流程阶段**：项目有明确流程时 → 前期准备、进行中、完成
5. **重要程度**：通用场景 → 核心工具、辅助工具、参考资源

### 分类维度参考

| 目录类型 | 推荐分类维度 |
|----------|--------------|
| 项目类（成立业委会） | 流程阶段：前期准备 → 进行中 → 完成 |
| 工具类（AI工具） | 功能：安装管理 → 内容生产 → 浏览器自动化 → 开发工具 |
| 知识类（AI学习） | 知识领域：基础 → 进阶 → 实战 |
| 个人类（leo） | 属性：账号 → 工作 → 阅读 → 成长 |
| 插件类（skill插件） | 功能：安装管理 → 内容生产 → 浏览器与搜索 → 开发工具 → AI行为约束 |

---

## 操作记录

记录到 `⚓新增文件记录.md`：

```markdown
### YYYY年M月D日
- 【优化】🧩 目录-xxx.md D:\Obsidian\LeoDiary\🧩 目录-xxx.md（重新分类+摘要补充）
```

---

## 状态追踪机制

### 追踪文件位置

```
logs/summary/summary-state.json
```

### 追踪数据结构

```json
{
  "版本": "1.0",
  "上次运行": "ISO 8601 时间戳",
  "目录状态": {
    "目录文件路径": {
      "路径": "🧩 目录-xxx.md",
      "标题": "目录文件的第一个 # 标题",
      "文件数": "当前文件数",
      "处理状态": "已更新 | 已跳过",
      "处理时间": "最后处理时间",
      "分类数": "分类数量",
      "摘要数": "摘要数量"
    }
  }
}
```

### 变化检测规则

**核心逻辑：标题变了 = 目录结构变了 = 必须重新整理**

| 优先级 | 条件 | 判断 | 动作 |
|--------|------|------|------|
| 1 | 目录文件不存在 | 新建 | 创建目录文件 |
| 2 | 目录文件的 # 标题与记录不同 | 修改（结构变了） | 重新整理 |
| 3 | 目录文件中的文件数变化 | 有增减 | 更新分类 |
| 4 | 以上全都不满足 | 未变化 | 跳过 |

**说明**：
- 目录文件内容修改但标题不变 → 跳过
- 标题修改 → 处理（目录结构变了）
- 文件数变化 → 处理（有新增或删除的文件）

### 执行后必须

1. 更新 `logs/summary/summary-state.json`
2. 写入运行日志 `logs/summary/summary-YYYY-MM-DD.md`
3. **更新 `📖目录 索引.md` 中对应条目的状态和 ✍️**

---

## 索引写入规则

整理目录文件后，**必须同步更新** `📖目录 索引.md`：

### 写入格式

```markdown
## 📁 分类名称
- [[文件名]] ✍️ 一句话总结 | 更新:YYYY-MM-DD | 状态:已整理
```

### 写入位置

在 `📖目录 索引.md` 中找到对应的分类区域，追加或更新条目。

### 状态值

| 状态 | 说明 |
|------|------|
| 已整理 | 文件已有 ✍️ 摘要 |
| 未整理 | 文件尚无 ✍️ 摘要 |
| 已移动 | 文件已被 Organizer 移动到正式目录 |

### 更新规则

| 情况 | 处理 |
|------|------|
| 文件在索引中不存在 | 追加新行 |
| 文件在索引中已存在但无 ✍️ | 更新行，添加 ✍️ |
| 文件在索引中已有 ✍️ | 跳过（保护规则） |

### 索引格式规范

```markdown
## 📁 {分类名称}
- [[文件名]] ✍️ 一句话总结 | 更新:YYYY-MM-DD | 状态:已整理
- [[另一个文件]] | 更新:待处理 | 状态:未整理
```

**字段说明**：

| 字段 | 说明 | 必填 |
|------|------|------|
| `[[文件名]]` | 双链 | ✅ |
| `✍️ 一句话总结` | 文件摘要 | ✅ |
| `更新:YYYY-MM-DD` | 最后更新时间 | ✅ |
| `状态:已整理/未整理/已移动` | 处理状态 | ✅ |

---

## 注意事项

- 编码必须为 UTF-8
- **允许修改源文件**：在标题后第二行插入 `>✍️` 摘要（这是 Step 1 的必须操作，不是"修改正文"）
- **禁止修改源文件正文**：除了第二行的 `>✍️` 摘要外，不改动源文件的其他内容
- 分类名称要简洁直观，符合"用户找文件时的第一反应"
- **禁止使用"其他"分类**：所有文件必须归入有意义的分类，禁止出现"未分类"或"其他"分类
- 每次操作完成后记录到日志
- **必须更新索引**：整理目录后，同步更新 `📖目录 索引.md` 中对应条目

## 🚫 防死循环规则（与Python脚本协调，强制）

**Python脚本（索引更新工具.exe等）负责结构维护，Skill负责内容加工。两边有重复工作正常，但必须避免死循环。**

### 🧩 目录文件分区规则

🧩 目录文件由 **Skill全权管理**，包含 frontmatter、分类整理、✍️摘要和所有文件链接。

```
# 🧩 目录-xxx          ← Skill生成（分类整理）
--- frontmatter ---     ← Skill生成
## 📌 一句话总结         ← Skill生成
## 📁 分类1             ← Skill生成
- [[文件]] ✍️ 摘要      ← Skill生成
## 📁 分类2             ← Skill生成
```

**规则**：
| 区域 | 管理者 | 操作 |
|------|--------|------|
| 整个目录文件 | **Skill** | 分类整理、✍️摘要、frontmatter、所有文件链接 |
| Python | 不涉及 | 不再处理目录文件，只维护 �目录 索引.md 的结构层 |



### 📖目录 索引.md 分区规则

```
# 📖目录 索引           ← Python生成
## 📂 目录结构树         ← Python生成（全量重写）
## 📊 统计摘要           ← Python生成
## 📋 按领域分组         ← Skill维护（分类+✍️摘要），Python只追加新文件
```

**规则**：
| 区域 | 管理者 | 操作 |
|------|--------|------|
| `## 📂 目录结构树` | **Python** | 全量重写（结构层） |
| `## 📊 统计摘要` | **Python** | 全量重写 |
| `## 📋 按领域分组` | **Skill** | 分类整理、✍️摘要、更新状态 |

**Skill更新"按领域分组"时**：
1. ✅ 在该区域内添加/更新分类和✍️摘要
2. ❌ **禁止修改** `## 📂 目录结构树` 和 `## 📊 统计摘要` 区域
3. ✅ Python会保留该区域已有内容，只追加新文件

### 标题修正规则

**Python的标题修正会跳过有frontmatter的文件。**

| 文件类型 | Python操作 | Skill操作 |
|----------|-----------|-----------|
| 无frontmatter的文件 | 确保第一行是`# 文件名` | 生成文件时确保标题正确 |
| 有frontmatter的文件 | **跳过**（不动标题） | 确保frontmatter后是`# 标题` |

**禁止**：Skill生成文件时不要加BOM，Python读取时用`utf-8-sig`会自动处理。

## 🚫 目录过滤规则（强制，防止误创建）

**必须基于物理文件系统遍历，禁止从 📖目录 索引.md 推断目录结构。**

以下目录/路径**完全跳过**，不生成 🧩 目录文件，也不处理其中内容：

| 目录/模式 | 说明 |
|-----------|------|
| `.` 开头的目录 | `.obsidian/`、`.git/`、`.trash/` 等隐藏目录 |
| `_trash/` | 回收站目录 |
| `D📦 归档（Archive）/processed-*/` | 归档子目录（按日期的 processed-YYYY-MM-DD） |
| **库根目录下的 LeoDiary 子目录** | 防止把索引中的 "LeoDiary" 名称误判为物理目录而创建嵌套文件夹 |

以下目录**仅处理到指定层级**：

| 目录 | 处理方式 |
|------|---------|
| `logs/` | 跳过整个 logs 目录（含所有子目录），不生成目录文件 |
| `A📥 收集（Capture）/` | 由 Pipeline 管理，本 Skill 不主动处理 |
| `B🧹 整理（Organize）/` | 由 Pipeline/Organizer 管理，本 Skill 不主动处理 |
| `C⚙️ 处理（Process）/` | 由 Pipeline/Compiler 管理，本 Skill 不主动处理 |

**验证机制**：处理任何目录前，必须先用 Glob/LS 确认该目录在物理上存在。目录不存在 → 跳过，不能因为索引里有条目就创建目录文件。

**根目录特殊规则**：库根目录（LeoDiary）只生成一个 `🧩 目录-LeoDiary.md`，不会创建 `LeoDiary/LeoDiary/` 这样的嵌套目录。

## 文件结构

```
C:\Users\leokou\.claude\skills\obsidian-mulu-fenlei-summary\
├── SKILL.md
├── agents/
│   └── openai.yaml
└── references/
    └── file-summary-rules.md
```


---

